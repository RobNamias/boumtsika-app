    Construire un composant avec une boucle (les SPANS) :
        for (let index=1; index<17;index++) {
            SPAN classname={"span_" + index}
            id={drumType.drumType + index}
        }

    Initialiser l'affichage du bpm
    Ne pas réinitialiser intervalBPM onChange bpm_setter

    Réussir à GetElementbyId.inner HTML Play/Pause et mettre/retirer la classe lecture_en_cours



